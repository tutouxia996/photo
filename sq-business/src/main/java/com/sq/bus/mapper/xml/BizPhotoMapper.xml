<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sq.bus.mapper.BizPhotoMapper">

    <select id="groupCountByDate" resultType="java.util.HashMap">
        SELECT DATE_FORMAT(shoot_time, '%Y-%m-%d') AS shootDate,
               COUNT(1) AS photoCount
        FROM biz_photo
        WHERE album_id = #{albumId}
          AND shoot_time IS NOT NULL
        GROUP BY DATE_FORMAT(shoot_time, '%Y-%m-%d')
        ORDER BY shootDate DESC
    </select>

</mapper>
