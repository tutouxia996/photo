# 本地密钥配置示例（不要提交真实 Key）
# 使用步骤：
# 1. 复制本文件为同目录下的 application-local.yml
# 2. 填入高德控制台的 Web 服务 Key
# 3. 重启后端（application.yml 已 optional 导入该文件，无需再加 local profile）
#
# 也可不建此文件，改为设置环境变量：
#   AMAP_WEB_KEY=你的Key
#   AMAP_JS_KEY=可选

album:
  map:
    type: amap
    webKey: 请填写你的高德Web服务Key
    jsKey:
